# AI 编程总结

这个项目从头到尾都在用 Claude Code 辅助开发。最大的感受是，AI 在"已经想清楚要做什么"的时候效率极高——改 UI、写配置、补测试，基本一句话就能拿到能跑的代码。但反过来，如果你是边写边想，AI 反而容易把你带偏，因为它太擅长"看起来合理"的答案了。

另一个体会是，AI 写的代码需要你对自己的项目有掌控力。它不知道哪些模块是你亲手调过的、哪些逻辑有隐式约束。如果盲目接受，很容易把之前埋的细节覆盖掉。所以我的习惯是让 AI 先跑通，然后自己逐段过一遍，把不该动的改回来。

总的来说，AI 像是一个不知疲倦的初级搭档——手速快、覆盖面广，但判断力在你手里。
